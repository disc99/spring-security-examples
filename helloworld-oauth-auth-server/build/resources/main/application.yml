spring:
  thymeleaf:
    cache: false

security:
  oauth2:
    client:
      client-id: demo
      client-secret: demo
      scope: openid
      authorized-grant-types: password,authorization_code
  user:
    password: password

server:
  port: 8081
  context-path: /uaa
